---
const { class: className = "", hero = false } = Astro.props;
---

<div
	class={`section-header stroke ${hero ? "section-header--hero" : ""} ${className}`.trim()}
>
    <div class="section-header__inner reveal reveal--up">        <slot />
    </div>
</div>

<style>
    .section-header {
        display: flex;
        padding-top: 0.5rem;
        flex-direction: column;
        align-items: flex-start;
        align-self: stretch;
        font-size: 1.5rem;
        line-height: 1;
    }

	.section-header--hero {
        padding-top: 2rem;
        font-size: clamp(2rem, 4.25cqi, 5.375rem);
    }

</style>